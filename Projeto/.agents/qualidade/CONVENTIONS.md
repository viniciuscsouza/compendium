# Template: Convenções e Qualidade de Código (CONVENTIONS.md)

Este documento estabelece as regras de estilo de código, formatação e boas práticas que devem ser seguidas em todo o projeto.
Agentes de IA devem aplicar essas convenções ao gerar ou refatorar qualquer trecho de código.

## 1. Guia de Estilo (Style Guide)
[Agente: Especifique se o projeto segue um guia de estilo reconhecido (ex: Airbnb, StandardJS, PEP-8) ou liste as regras customizadas (espaçamento, aspas simples/duplas, ponto e vírgula).]

## 2. Ferramentas de Linter e Formatação
[Agente: Indique as ferramentas utilizadas no projeto (ex: ESLint, Prettier, SonarQube, Flake8) e instrua a IA a respeitar as regras configuradas nesses arquivos (`.eslintrc`, `.prettierrc`).]

## 3. Padrões de Nomenclatura (Naming Conventions)
[Agente: Defina as regras para nomear variáveis, funções, classes, constantes e interfaces. Exemplo: Classes em `PascalCase`, variáveis booleanas com prefixo `is/has`, constantes em `UPPER_SNAKE_CASE`.]

## 4. Tratamento de Erros e Exceções
[Agente: Como os erros devem ser capturados e retornados? (Ex: uso de blocos try/catch, retorno de objetos de erro customizados, códigos HTTP adequados).]

## 5. Comentários e Documentação de Código
[Agente: Defina o padrão para documentar funções (ex: JSDoc, Docstrings) e quando comentários de linha/bloco são encorajados ou desencorajados.]
