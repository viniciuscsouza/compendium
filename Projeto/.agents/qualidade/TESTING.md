# Template: Estratégia e Padrões de Testes (TESTING.md)

Este documento detalha como os testes são escritos, estruturados e executados dentro do projeto.
Agentes de IA devem utilizar essas diretrizes para gerar novos testes ou atualizar os existentes adequadamente.

## 1. Ferramentas e Frameworks
[Agente: Informe o ecossistema de testes em uso (ex: Jest, Mocha, PyTest, Cypress) e utilitários auxiliares (ex: React Testing Library, Sinon).]

## 2. Estratégia e Pirâmide de Testes
[Agente: O projeto foca mais em testes unitários, integração ou E2E? Defina as proporções esperadas e quais camadas do software devem ser testadas prioritariamente.]

## 3. Organização e Nomenclatura de Arquivos de Teste
[Agente: Onde os testes devem ficar? (Ex: na mesma pasta do arquivo testado com a extensão `.test.js` / `.spec.js`, ou em uma pasta `__tests__` ou `tests/` separada na raiz?)]

## 4. Padrões de Escrita (Anatomia do Teste)
[Agente: Descreva a estrutura esperada para os blocos de teste. Recomenda-se o padrão Arrange-Act-Assert (AAA) ou Given-When-Then. Forneça exemplos de blocos `describe` e `it`/`test`.]

## 5. Uso de Mocks, Stubs e Fixtures
[Agente: Como lidar com dependências externas (APIs, Bancos de Dados, Time/Date)? Defina regras sobre quando *mockar* e quando usar dependências reais ou em memória.]
