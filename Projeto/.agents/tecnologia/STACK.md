# Template: Stack Tecnológica (STACK.md)

Este documento registra todas as linguagens, frameworks e bibliotecas fundamentais (core) que compõem o ecossistema do projeto.
Agentes de IA devem utilizar apenas as ferramentas listadas aqui, a menos que instruídos explicitamente a sugerir novas bibliotecas.

## 1. Core Stack
[Agente: Liste a linguagem de programação principal e a versão (ex: Node.js v20, Python 3.11) e o framework central (ex: Next.js 14, Django 5, Spring Boot 3).]

## 2. Banco de Dados e Armazenamento
[Agente: Especifique os bancos de dados relacionais e NoSQL utilizados (ex: PostgreSQL, MongoDB), bem como caches (ex: Redis) e a ORM/ODM escolhida (ex: Prisma, TypeORM, SQLAlchemy).]

## 3. Gerenciador de Pacotes
[Agente: Qual gerenciador e versão devem ser usados? (npm, yarn, pnpm, pip, poetry, maven). Isso previne que a IA sugira comandos que quebrem os `.lock` files.]

## 4. Bibliotecas Utilizadas / Dependências Chave
[Agente: Liste as libs mais vitais para o domínio (ex: `date-fns` para datas, `zod` para validação de esquemas). Instrua a IA a NÃO propor bibliotecas concorrentes (ex: não sugerir `moment.js` se já usamos `date-fns`).]

## 5. Ferramental de Build e CI/CD
[Agente: O projeto usa Vite, Webpack, Babel, Docker? Em qual plataforma está o pipeline (GitHub Actions, GitLab CI)?]
