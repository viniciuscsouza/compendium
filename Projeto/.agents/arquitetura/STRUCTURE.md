# Template: Estrutura de Diretórios (STRUCTURE.md)

Este documento mapeia a organização física dos arquivos no repositório.
Agentes de IA devem utilizar este documento para saber exatamente onde criar, editar ou localizar arquivos específicos baseados em sua responsabilidade.

## 1. Árvore de Diretórios Principal
[Agente: Apresente a estrutura macro de diretórios do projeto usando formatação de texto. Exemplo:
```text
src/
├── controllers/
├── models/
└── views/
```
]

## 2. Mapa de Responsabilidades
[Agente: Para cada diretório principal, forneça uma breve explicação de que tipo de código deve ser colocado ali.]
- `[Nome do Diretório 1]`: [Explicação do que vai aqui]
- `[Nome do Diretório 2]`: [Explicação do que vai aqui]

## 3. Regras de Nomenclatura de Arquivos
[Agente: Defina como os arquivos devem ser nomeados em diferentes partes do sistema (ex: `kebab-case` para componentes, `camelCase` para utilitários, sufixos obrigatórios como `.controller.ts`).]

## 4. Locais Restritos / Módulos Fechados
[Agente: Se houver pastas ou arquivos que NÃO devem ser alterados pelas IAs ou que exigem permissão especial, liste-os aqui claramente.]
